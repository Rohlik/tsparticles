export function loadBasicPreset(tsParticles) {
    tsParticles.addPreset("basic", {
        particles: {
            lineLinked: {
                enable: true,
            },
            move: {
                enable: true,
            },
        },
    });
}
